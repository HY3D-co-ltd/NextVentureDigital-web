/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/NextVentureDigital-web',
  images: {
    unoptimized: true,
  },
  trailingSlash: true,
};

export default nextConfig;
